# WhatsApp Bot configuration
ENGINE_URL=http://engine:8000

# Must match API_KEY set in engine/.env (leave empty if engine auth is disabled)
ENGINE_API_KEY=

# Path to store Baileys auth session data
SESSION_DATA_PATH=./auth_info

# Logging
LOG_LEVEL=info
